{
  "name": "phantom_logger",
  "version": "1.0.0",
  "description": "A bot to log what people say on the discord server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord-message-handler": "^1.0.4",
    "discord.js": "^11.3.2",
    "fs": "0.0.1-security",
    "request": "^2.85.0"
  }
}
